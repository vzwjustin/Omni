{
  "mcpServers": {
    "omni-cortex": {
      "command": "docker",
      "args": [
        "run", "--rm", "-i",
        "--env-file", "${CLAUDE_PLUGIN_ROOT}/.env",
        "-v", "${CLAUDE_PLUGIN_ROOT}/data:/app/data",
        "vzwjustin/omni-cortex:latest"
      ]
    }
  }
}
