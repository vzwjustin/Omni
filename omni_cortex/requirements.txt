# Omni-Cortex MCP Server Dependencies

# =============================================================================
# MCP Server
# =============================================================================
mcp[cli]>=1.0.0

# =============================================================================
# LangGraph & LangChain
# =============================================================================
langgraph>=0.2.0
langgraph-checkpoint-sqlite>=1.0.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-anthropic>=0.2.0
langchain-openai>=0.2.0
langchain-chroma>=0.2.0
chromadb>=0.5.3
watchfiles>=0.21.0
watchdog>=3.0.0  # File system monitoring for cache invalidation

# =============================================================================
# LLM Providers
# =============================================================================
# Google AI (Gemini models - recommended for cost)
google-genai>=0.3.0  # NEW: Supports thinking mode (extended reasoning)
google-generativeai>=0.8.0  # DEPRECATED: Fallback only
langchain-google-genai>=4.0.0

# OpenRouter (uses OpenAI SDK with custom base_url)
# No separate package needed - uses openai package

# Anthropic (Claude models)
anthropic>=0.40.0

# OpenAI (GPT models, also used for OpenRouter)
openai>=1.50.0

# =============================================================================
# Core
# =============================================================================
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0.0

# =============================================================================
# Async & HTTP
# =============================================================================
aiohttp>=3.9.0
httpx>=0.27.0
# Security: Pin urllib3 to fix CVEs (decompression bombs, DoS)
urllib3>=2.6.3

# =============================================================================
# Utilities
# =============================================================================
tenacity>=8.0.0
structlog>=24.0.0

# =============================================================================
# Observability
# =============================================================================
prometheus-client>=0.20.0

# =============================================================================
# Dataset Ingestion (Optional)
# =============================================================================
pandas>=2.0.0  # For CSV parsing in bug-fix datasets
datasets>=2.15.0  # For loading HuggingFace datasets
