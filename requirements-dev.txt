# Development dependencies for Omni Cortex

# Core runtime deps (so pytest/conftest imports work)
-r omni_cortex/requirements.txt

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0

# Code formatting and linting
black==24.3.0
isort==5.13.2
mypy==1.8.0
